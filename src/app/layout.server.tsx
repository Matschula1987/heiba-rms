import { metadata } from './metadata'

// Server-seitige Layout-Komponente für Metadaten
// Diese Komponente wird vom Next.js Server-Renderer verwendet

export { metadata }

// Dies ist eine leere Wrapper-Komponente, die keine Rendering-Funktion enthält,
// sondern nur die Metadaten exportiert. Die eigentliche Layout-Komponente
// befindet sich in layout.tsx
